package accessspecifier;
public class strings {
 String str="ramya";
	public static void main(String[] args) {
		String str="ramya";
		int maxcount=0;char maxchar=str.charAt(0);
		for(int i=0;i<str.length();i++)
		{
			char ch1=str.charAt(i);
			int caount char=count occurance(str,ch);
			if(maxcount<counchar)
			{maxcount=countchar;
			maxchar=ch1;
			}
		}
		

	}

}
